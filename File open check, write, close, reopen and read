#include <fstream>
#include <iostream>
using namespace std;

int main() {
    ofstream out("data.txt");
    if (out.is_open()) {
        out << "Hello File";
        out.close();
    }

    ifstream in("data.txt");
    string s;
    getline(in,s);
    cout << s << endl;
    in.close();
}
