#include <iostream>
using namespace std;

class Student {
public:
    int id;
    string name;
};

int main() {
    int n = 2;
    Student* s = new Student[n];

    s[0] = {1,"A"};
    s[1] = {2,"B"};

    for(int i=0;i<n;i++)
        cout << s[i].id << " " << s[i].name << endl;

    delete[] s;
}
